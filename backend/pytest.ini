[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    slow: needs the real sentence-transformers model (downloads ~90 MB on first run)
filterwarnings =
    ignore::DeprecationWarning
